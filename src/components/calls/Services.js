import React from "react";
import CardInfo from "../cards/CardInfo";

const Services = props => {
  return (
    <CardInfo
      title="Take Your Business to The Next Level"
      text="Let me design and build you a highly converting, mobile first, secure and optimized website today."
      btnText="Check Out My Services"
      btnLink="/services"
      addClass="landing-services"
    />
  );
};

export default Services;
